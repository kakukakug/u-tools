// import React from "react";
// import { render } from "test-utils";

// import { IconsScreenUI } from "../../../../src/04_external/components/05_pages/IconsScreen/ui";

describe("IconsScreenUI", () => {
  it(`test 実装が必要`, () => {
    expect(true).toBeTruthy();
  });
  // it(`IconsScreenUIを描写`, () => {
  // const mockSetVisibleIcons = jest.fn;
  // const component = render(
  // <IconsScreenUI
  // // icons={["down", "up", "left", "right"]}
  // icons={[]}
  // setVisibleIccons={mockSetVisibleIcons}
  // />
  // );

  // expect(component.toJSON()).toMatchSnapshot();
  // });
});
