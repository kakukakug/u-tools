// import React from "react";
// import { render, fireEvent } from "@testing-library/react-native";

// import { IconItem } from "../../../../src/04_external/components/01_atoms/IconItem";

describe("IconItem", () => {
  it(`test 実装が必要`, () => {
    expect(true).toBeTruthy();
  });
  // const mockOnPress = jest.fn();
  // const mockName = "star";
  // const mockFamilyName = "Ionicons";
  // it(`IconItemを描写`, () => {
  // const component = render(
  // <IconItem
  // name={mockName}
  // onPress={mockOnPress}
  // selectFamily={mockFamilyName}
  // />
  // );

  // const name = component.getByText(mockName);
  // expect(name).toBeTruthy();

  // fireEvent.press(name);
  // expect(mockOnPress).toBeCalled();

  // expect(component.toJSON()).toMatchSnapshot();
  // });
});
